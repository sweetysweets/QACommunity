<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nju.zhihu.Dao.UserDao">

    <select id="getUserById" resultType="User">
        SELECT
        user_id as id,
        token as token,
        nickname as name,
        avater as avater
        FROM user
        WHERE token = #{id}
    </select>

    <select id="getFollowUserById" resultType="User">
        SELECT
        b.user_id as id,
        token as token,
        nickname as name,
        avater as avater
        FROM follow_user a, user b
        WHERE a.user_id = #{id} and a.user_followed_id = b.user_id ;
    </select>

    <resultMap id="getQuestionAndAnswerByUserId" type="com.nju.zhihu.Entity."
    <select id="getQuestionByUserId" resultType="Question">
        SELECT
        b.question_id as qid,
        b.user_id as uid,
        b.title as title,
        b.content as content,
        b.time as time,
        b.state as state,
        a.avater as user_avater,
        a.nickname as user_name
        FROM user a, question b
        WHERE a.user_id = #{id} and a.user_id = b.user_id
        ORDER BY b.time DESC;
    </select>
    <!--<update id="updateUser">-->
        <!--UPDATE user-->
        <!--SET NAME = #{user.name},-->
        <!--SEX = #{user.sex},-->
        <!--AGE = #{user.age}-->
        <!--WHERE ID = #{user.id}-->
    <!--</update>-->
    <insert id="addUser">
        INSERT INTO
        user(nickname, avater,token)
        VALUES (
        #{user.name},
        #{user.avater},
         #{user.token}
        )
    </insert>
    <!--<delete id="deleteUserById">-->
        <!--DELETE user WHERE ID = #{id}-->
    <!--</delete>-->

</mapper>